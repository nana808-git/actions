version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Installed Nodejs .....
  pre_build:
    commands:
     - echo Build started on `date`
  build:
    commands:
      - echo Build started on `date`
    
